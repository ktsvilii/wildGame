import { FC } from 'react';

const Home: FC = () => {
  return (
    <div className='flex flex-grow min-h-dvh items-center justify-center'>
      <button>Hello DaisyUI!</button>
    </div>
  );
};

export default Home;
